import TinyversePage from '../../page';

export default function ArticlePage() {
  // Render the main TinyversePage component which will read the URL path
  // and display the correct article based on the [section] and [article] params
  return <TinyversePage />;
}
